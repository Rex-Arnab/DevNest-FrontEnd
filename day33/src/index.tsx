import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App title="Page Title" discription="Typescript React App" />
  </React.StrictMode>,
  document.getElementById('root')
);